# Hermes Client Configuration
# ----------------------------
# This file is normally written by `hermes-client configure`.
# You can also create it manually and place it at one of:
#   .env.hermes-client                        (current working directory)
#   ~/.env.hermes-client                      (home directory)
#   %APPDATA%\Hermes\.env.hermes-client       (Windows — preferred)

SERVER_URL=http://your-build-server:8000
CLIENT_NAME=My Workstation
LOCAL_HOST=0.0.0.0
LOCAL_PORT=9000

# Callback URL — the Hermes server POSTs notifications here.
# `hermes-client configure` detects this automatically from your LAN IP.
CALLBACK_URL=http://192.168.1.100:9000/notify

# AzDO credentials — used to resolve your identity at configure time.
# `hermes-client configure` fills AZDO_USER_ID and AZDO_DISPLAY_NAME in automatically.
AZDO_ORGANIZATION_URL=http://your-ado-server/DefaultCollection
AZDO_PAT=your_personal_access_token_here
AZDO_USER_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
AZDO_DISPLAY_NAME=Dale

SUBSCRIPTIONS=["pr", "workitem", "pipeline", "manual"]
