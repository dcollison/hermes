# Hermes Server Configuration
# ----------------------------
# This file is normally placed at one of:
#   .env.hermes-server          (current working directory)
#   %APPDATA%\Hermes\           (Windows — preferred for installed package)
#
# A plain .env in the current directory is also accepted for backwards compatibility.

# Server bind settings
HOST=0.0.0.0
PORT=8000

# The public URL of this server (shown in setup instructions)
SERVER_PUBLIC_URL=http://your-build-server:8000

# Azure DevOps Server (on-premises)
ADO_ORGANIZATION_URL=http://your-ado-server/DefaultCollection
ADO_PAT=your_personal_access_token_here

# Optional: shared secret for webhook validation.
# Set the same value in ADO service hook settings → Filters → Resource version secret.
# ADO_WEBHOOK_SECRET=your_shared_secret_here

# Directory where clients.json and notifications.log are stored.
# Relative paths are resolved from the working directory.
DATA_DIR=data

# Rotate notifications.log when it reaches this size in bytes (default: 5 MB)
LOG_MAX_BYTES=5242880

# Number of rolled log files to keep (default: 3)
# e.g. notifications.log.1, notifications.log.2, notifications.log.3
LOG_BACKUP_COUNT=3
